# Save-Token 最佳实践（实测验证）

> 基于 2026-05-19 实际测试数据生成。所有建议均经过 `st ask` / `st run` 验证。

---

## 一、什么任务应该用 `st` 省 Token

### 判断标准：能否用免费 AI 替代

| 任务类型 | 用 `st` | 用 Hermes/API | 原因 |
|----------|---------|---------------|------|
| 代码生成（单函数/小脚本） | ✅ `st ask --local` | ❌ | 零成本，2.6s |
| 代码生成（复杂架构） | ✅ `st ask -p deepseek` | ❌ | 免费，38s |
| 查询实时信息（天气/新闻） | ✅ `st ask -p yuanbao` | ❌ | 元宝8s，实时数据 |
| 概念解释（什么是XX） | ✅ `st ask --local` | ❌ | 本地模型够用 |
| 文件分析 | ✅ `st ask -f file.py` | ❌ | 免费上传分析 |
| 多任务流水线 | ✅ `st run "1. 2. 3."` | ❌ | 同一Chat逐轮 |
| 需要工具调用（写文件/运行命令） | ❌ | ✅ | `st` 无工具能力 |
| 复杂多步推理 | ❌ | ✅ | `st` 上下文限制 |
| 需要读项目文件/改代码 | ❌ | ✅ | `st` 无文件系统访问 |

### 实测对比

```
任务: 写一个二分查找函数
  st --local:  11.7s, 零Token, 代码正确 ✅
  st -p deepseek: 27.8s, 零Token, 代码更优 ✅
  Hermes(GPT-5): ~5s, 花Token, 代码最优

任务: 查询今天日期
  st --local:  3.3s, 零Token, 结果错误 ❌ (编造了"2023年4月1日")
  st -p yuanbao: 8s, 零Token, 结果正确 ✅
```

---

## 二、提示词怎么写让输出干净

### 实测：不同 prompt 的输出质量

#### ❌ 差：模糊提问
```
st ask "Python装饰器是什么"
→ 输出 489 字符，包含冗余解释和发散内容
```

#### ✅ 好：加约束条件
```
st ask "Python装饰器是什么。要求：1.三句话内解释 2.给一个代码例子 3.不要展开"
→ 输出紧凑，直奔主题
```

### 黄金模板

```bash
# 只输出代码
st ask "写Python快排。只输出代码，不要解释" -p deepseek

# 简洁解释 + 代码
st ask "什么是闭包。一句话解释+一个例子。不要长篇大论" -p deepseek

# 文件分析
st ask "分析时间复杂度。只给结论和复杂度级别" -f ./src/main.py

# 多子任务（合理拆分）
st run "1. 解释概念A 2. 给代码例子 3. 指出常见错误"

# 实时信息
st ask "今天深圳天气" -p yuanbao
```

### Prompt 技巧

| 技巧 | 示例 | 效果 |
|------|------|------|
| 限定字数 | "三句话内" | 减少80%冗余 |
| 要求格式 | "只输出代码" | 去掉解释 |
| 禁止发散 | "不要展开" | 聚焦主题 |
| 指定角色 | "你是Python专家" | 提高质量 |
| 给出模板 | "用这个格式: [答案]" | 结构化输出 |

---

## 三、何时另起 Chat vs 保持同一 Chat

### 决策树

```
问题是独立的？
  ├── 是 → st ask（新Chat）
  └── 否 → 后续问题依赖前面的答案？
            ├── 是 → st run（同一Chat逐轮）
            └── 否 → st ask（新Chat，避免上下文污染）
```

### 实测对比

#### 保持上下文 ✅（用 `st run`）
```bash
st run "1. Python的GIL是什么 2. 如何绕过GIL"
# Turn 2 自动知道 Turn 1 的内容
# 实测: 27秒, 2轮对话, AI说"用户已经知道GIL是什么" → 上下文成功
```

#### 独立上下文 ❌（用两次 `st ask`）
```bash
st ask "Python的GIL是什么"
st ask "如何绕过GIL"
# 第二次提问需要重新解释GIL → 浪费
# 实测: 79秒, 第二次回答仍包含GIL基础解释
```

### 规则

| 场景 | 用 | 原因 |
|------|-----|------|
| 3个独立问题 | 3次 `st ask` | 互不依赖 |
| 递进式问题 | `st run "1. 2. 3."` | 上下文传递 |
| 长对话(>5轮) | 拆分 `st run` | 避免上下文过长 |
| 分析文件后追问 | `st run` | 文件在上下文里 |
| 切换话题 | 新 `st ask` | 避免污染 |

---

## 四、Provider 选择指南

| Provider | 速度 | 适用场景 | 特点 |
|----------|------|---------|------|
| `--local` | 2-6s | 代码生成、概念解释 | 零成本，离线，1.5B能力有限 |
| `-p yuanbao` | 8s | 实时信息、天气、新闻 | 元宝擅长实时数据 |
| `-p doubao` | 6s | 快速问答 | 最快免费provider |
| `-p deepseek` | 30-60s | 编程、技术问题、深度分析 | 质量最高，支持expert模式 |
| `-p kimi` | 30s | 长文本分析 | 128K上下文 |

---

## 五、实战 Checklist

```bash
# 1. 先试本地（零成本）
st ask "问题" --local

# 2. 本地不行 → 元宝（快速+实时）
st ask "今天天气" -p yuanbao

# 3. 需要高质量 → DeepSeek
st ask "复杂技术问题" -p deepseek --expert

# 4. 多步任务 → st run  
st run "1. 分析问题 2. 设计方案 3. 写代码"

# 5. 分析文件
st ask "分析复杂度" -f ./src/main.py -p deepseek

# 6. 保存结果
st ask "写代码" -p deepseek -o ./output.py

# 7. 卡住了 → 问 AI
st ask "如何解决XXX错误" -p deepseek --expert
```
